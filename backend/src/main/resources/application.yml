server:
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    root: DEBUG
    com.tictactoe: DEBUG

spring:
  datasource:
    password: ravdes123
    url: jdbc:postgresql://localhost:5432/tictacdb
    username: ravdes
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
       format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: tictactoe21online@gmail.com
    password: qcmb vife rnkp bodj
    properties:
      mail:
        smtp:

          auth: true
          starttls:
            enable: true
